[project]
name = "storm-proj"
version = "0.1.0"
description = "Map live weather predictions to a map"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "pandas>=3.0.3",
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.51",
]
